on:
  workflow_call:

env:
  CARGO_TERM_COLOR: always

jobs:
  use:
    runs-on: ubuntu-latest
    steps:
      # - uses: actions/download-artifact@v4
      #   with:
      #     name: binary
      #     path: /tmp/
      # - run: tar -xvf /tmp/hello-bin.tar -C /
      - uses: actions/download-artifact@v4
        with:
          name: binaries
          path: /tmp/
      - run: tar -xvf /tmp/target.tar # -C /
      - run: hello-bin
